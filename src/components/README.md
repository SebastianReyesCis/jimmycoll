Componentes disponibles:

- `Navbar` - barra de navegación
- `Carousel` - carrusel reutilizable (importa `Carousel.css` automáticamente)

Importar desde `./components`:

```js
import { Navbar, Carousel } from './components';
```
